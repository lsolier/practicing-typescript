(function() {
    const nombre = 'Luis';
    const apellidos = 'Solier';
    const edad = 26;
    const salida = nombre + ' ' + apellidos + ' (' + edad + ')';
    console.log(salida);
    const print = `${ nombre } ${ apellidos } ${edad}`;
})();

